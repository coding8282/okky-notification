version: 0.2

phases:
  build:
    commands:
      - mvn clean install -Pprod

artifacts:
  files:
    - integration/appspec.yml
    - integration/scripts/*
    - target/okky-notification-1.0.0.jar
  discard-paths: yes

cache:
  paths:
    - '/root/.m2/**/*'